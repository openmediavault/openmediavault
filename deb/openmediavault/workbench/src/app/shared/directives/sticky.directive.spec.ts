import { StickyDirective } from '~/app/shared/directives/sticky.directive';

describe('StickyDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyDirective(null);
    expect(directive).toBeTruthy();
  });
});
